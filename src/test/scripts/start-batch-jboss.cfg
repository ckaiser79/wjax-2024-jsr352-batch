# start the batch and exit
# Pfad anpassen!
/deployment=example-file2file-batch.war/subsystem=batch-jberet:start-job( \
    job-xml-name=demo-file2db-batch.xml, \
    properties={ \
        quell_datei=../resources/persons.txt
    } \
)